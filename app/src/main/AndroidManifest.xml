<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rummezheni.price_history" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.rummezheni.price_history.AddActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".MyListActivity"
            android:label="@string/title_activity_item_list" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.example.rummezheni.price_history.MyContentProvider"
            android:authorities="com.training.contentprovidersample" >
        </provider>

    </application>

</manifest>
